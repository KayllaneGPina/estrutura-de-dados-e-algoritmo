# No Python não precisamos definir o tipo de uma variável porque a linguagem já dinamicamente tipada
frase = 'Boa noite!'
print(type(frase))

# Já quando falamos de constantes, nativamente no Python não temos, o que acontece é que existem convenções e em Python, uma constante é definida sendo escrita em letra maiúscula
NOME_PESSOA = 'Kayllane'
print(type(NOME_PESSOA))