import numpy as np
matrix = np.array([
    [2, 3, 1, 6],
    [4, 5, 7, 8],
    [0, 9, 12, 69],
    [23, 54, 48, 15]
])

print(matrix)
print(matrix[0][0])
print(matrix.shape) # Retorna o formato da matriz que neste caso é 2x3 (2 linhas e 3 colunas)
 

